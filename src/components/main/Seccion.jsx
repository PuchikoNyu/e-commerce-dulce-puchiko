import React from "react";
import Carrousel from "./Carrousel";
import Cards from "./Cards";

const Seccion = () => {
    return(
        <section>
            <Carrousel />
            <Cards />
        </section>
    );
};

export default Seccion;